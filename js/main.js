$(function(){
	$(".top__slider").slick({
		arrows: false, //отключаем видимость стрелок
		dots: true, // включаем дотсы
		autoplay: true, // слайдер самостоятельно меняется
		fade: true //плавная смена слайдов
	});
});